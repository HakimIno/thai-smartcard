# Cross-compilation configuration for pcsc-lite dependencies
# Note: musl target is no longer used. We use gnu binary with gcompat on Alpine instead.

# [target.x86_64-unknown-linux-musl]
# pre-build = [
#     "apt-get update",
#     "apt-get install -y libpcsclite-dev:amd64 pkg-config",
# ]
