# Cargo configuration for cross-compilation

[env]
# Allow pkg-config to work in cross-compilation environments
PKG_CONFIG_ALLOW_CROSS = "1"

# Note: musl target configuration removed as we use gnu binary with gcompat on Alpine instead
# [target.x86_64-unknown-linux-musl]
# rustflags = ["-C", "target-feature=+crt-static", "-C", "link-arg=-static-libgcc"]

